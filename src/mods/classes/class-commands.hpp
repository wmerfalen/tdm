#ifndef __MENTOC_MODS_CLASS_COMMANDS_HEADER__
#define  __MENTOC_MODS_CLASS_COMMANDS_HEADER__

ACMD(do_go_dark); // calls go_stealth
ACMD(do_conceal); // apply_stealth_to_*
ACMD(do_feign_death);
ACMD(do_summon_extraction);
ACMD(do_xray_shot);

ACMD(do_teep);
#endif
