# type: AMMUNITION
# type: FOOD
# type: PILLS
# type: HERBS
# type: HALLUCINOGENS
# type: PEDS
# type: GASOLINE
# type: TRANQUILIZER_DARTS
csv_capabilities: 'TBA' #csv, strings free-form TBA
csv_attach_to: 'TBA' #csv, strings attach to various kinds of items TBA
weight_in_lbs: 2 #int, weight in lbs
consumed_by: 2 #string, ENUM HUMAN,VEHICLE,ATTACHMENT,DRONE,GADGET,RIFLE,TRAP 
adds_hp: 0 #int,  amount of hp it adds when consumed (can be negative)
adds_movement: 0 #int, amount of movement it adds when consumed (can be negative)
adds_room_range: 0 #int, amount of rooms it adds when consumed (can be negative)
adds_strength: 0 #int, amount of STR it adds when consumed (can be negative)
adds_intelligence: 0 #int, amount of INT it adds when consumed (can be negative)
adds_charisma: 0 #int, amount of CHA it adds when consumed (can be negative)
adds_dexterity: 0 #int, amount of DEX it adds when consumed (can be negative)
adds_constitution: 0 #int, amount of CON it adds when consumed (can be negative)
adds_wisdom: 0 #int, amount of WIS it adds when consumed (can be negative)
adds_weight: 0 #int, amount of weight it adds when consumed (can be negative)
adds_experience: 0 #int, amount of EXP it adds when consumed (can be negative)
adds_armor_class: 0 #int, amount of AC it adds when consumed (can be negative)
adds_hitroll: 0 #int, amount of HITROLL it adds when consumed (can be negative)
adds_damage_roll: 0 #int, amount of DAMROLL it adds when consumed (can be negative)
ticks_until_zero: 0 #int, amount of game ticks until consumable wears off (can be negative)
ammo_type: 'NONE' #string, ENUM any type of ITEM_RIFLE if this is ammunition
capacity: 0 #int, usually used for ammunition. how much of this consumable is present (i.e.: bullets)
adds_fire_damage: 0 #int, amount of fire damage it adds to the consumer
adds_critical_chance: 0 #int, amount of points to add to critical chance of consumer
adds_ammo_max: 30 #int, adds to ammo maximum of consumer
adds_chance_to_injure: 10 #float, out of 100
adds_clip_size: 7 #int, bullets
adds_cooldown_between_shots: 0 #int, ticks (can be negative)
adds_critical_range: 3 #int, rooms (can be negative)
adds_disorient_amount: 3 #float, out of 100
adds_headshot_bonus: 48 #float, static addition to hitroll
adds_max_range: 8 #int, rooms
adds_reload_time: 3 #int, ticks
adds_muzzle_velocity: 1 #int, TBA
adds_damage_dice_count: 2 #int, number of dice
adds_damage_dice_sides: 6 #int, dice sides
str_type: 'PED' # enum, object type
manufacturer: 'ACME Industries' # string, anything 
name: 'N7RCX Human Growth Hormone ' # string, anything name of object 
vnum: 1000 # integer, unique identifier
description: 'Description'
short_description: 'short description'
action_description: 'action description'
feed_file: 'PED.yml'
#rarity types
# COMMON
# UNCOMMON
# RARE
# LEGENDARY
# GOD_TIER
# DEFAULT
rarity: 'COMMON'
